# This file is a template managed by Chezmoi, it must be edited with `chezmoi edit`.
# ---------------------------------

{{- if eq .where "work" }}
github.com:
    user: LaurentHrt
    git_protocol: ssh
    users:
        LaurentHrt:
github-prod.sidel.com:
    users:
        laurent-hirth:
            oauth_token: {{ if eq .bwActivated "yes" -}} {{ (bitwarden "item" "github-sidel-oauth-token").notes }} {{- end }}
    git_protocol: ssh
    user: laurent-hirth
    oauth_token: {{ if eq .bwActivated "yes" -}} {{ (bitwarden "item" "github-sidel-oauth-token").notes }} {{- end }}
{{- end }}
{{- if eq .where "home" }}
github.com:
    user: LaurentHrt
    git_protocol: ssh
    users:
        LaurentHrt:
{{- end }}
